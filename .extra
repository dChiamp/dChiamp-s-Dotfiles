#add personal $PATH bc
#1) it shoudn't be the same across your machines or
# 2) it shouldn't be in a git repo

#example:

#	  PATH = /usr/local/bin
#PATH=$PATH:/usr/bin
#PATH=$PATH:/bin
#PATH=$PATH:/usr/sbin
#PATH=$PATH:/sbin

#export PATH
